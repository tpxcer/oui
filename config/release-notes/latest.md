## OUI 2026.6.22

- 优化 Xray 设置里的入站标签选择：路由规则和 loopback 出站选择入站标签时，会在标签后显示对应入站备注，避免只看到 `inbound-端口` 而选错节点。
- 新增入站标签备注映射接口字段，前端仅用于下拉展示；实际保存到 Xray 配置里的值仍保持原始入站标签，不影响现有规则。
- 补充入站标签备注过滤测试，确保只返回当前用户、已启用、本机入站的备注信息。
