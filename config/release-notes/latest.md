## OUI 2026.6.16

- 修复面板更新到 `2026.6.15-4` 时卡在 95% 并提示更新失败的问题：发布包里的程序版本号没有随 tag 更新，安装校验会读到旧版本 `2026.6.15-2`。
- Release 构建流程现在会在 tag 发版时自动把 `config/version` 写成当前 tag，避免以后再次出现“下载的是新包、二进制自报旧版本”的情况。
- 保留 `2026.6.15-4` 的入站/客户端自动刷新修复，新版本可从旧版直接更新。
