## OUI 2026.9.16

- 首页顶部状态区恢复为三个等宽实时趋势图，撤回上一版的单通栏六曲线布局。
- 第一个分区显示 `CPU` 与存储，第二个分区显示上传与下载，第三个分区显示内存与交换分区。
- 桌面端保持三等分，平板自动调整为两栏加一块通栏，手机自动调整为单栏；详细容量可在图例悬浮查看。
- 升级 `gRPC` 至安全版本，修复 HTTP/2 DATA 分片可能导致内存耗尽的问题。
