## OUI 2026.6.18

- 修复 IP 限制超过上限后，同一个已掐断 IP 在后续扫描中可能重复发送“超出 IP 上限，已掐断”Telegram 通知的问题。
- 已处于封禁状态的 `用户 + IP + 端口` 不再重复写入 BAN 记录，也不会再次触发掐断通知。
- 收紧 IP 限制同步解封逻辑：只有当前保留 IP 数量小于面板限制时，才会从已封禁 IP 中按首次出现时间补位解封，避免自动解封后又重新封禁造成通知噪音。
