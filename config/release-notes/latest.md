## OUI 2026.8.30-1

- 删除节点后显示与一键创建一致的防火墙处理提示，明确列出对应端口或 Hysteria2 端口跳跃范围的关闭结果。
- 多个节点共用端口时提示该端口仍在使用并予以保留；没有 OUI 自动放行记录时提示无需关闭，不会误删手动放行规则。
- 防火墙关闭失败时改用警告提示，并说明面板启动后会自动重试，避免把失败操作显示为成功。
- 单个删除和批量删除均支持防火墙结果汇总；批量操作只显示一次汇总通知，避免连续弹出重复提示。
